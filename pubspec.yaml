name: blog_app
description: Blog App — Educational project for Code Smarter With AI course
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  supabase_flutter: ^2.8.0
  dartz: ^0.10.1
  get_it: ^8.0.2
  shared_preferences: ^2.3.3
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  flutter_lints: ^5.0.0
